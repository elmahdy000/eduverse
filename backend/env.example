# Database
DATABASE_URL=postgresql://eduvers:replace_with_a_strong_password@localhost:5432/eduvers

# JWT Configuration
JWT_SECRET=replace_with_at_least_32_random_characters
JWT_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# Server
PORT=3001
NODE_ENV=development

# CORS
FRONTEND_ORIGINS=http://localhost:3000
